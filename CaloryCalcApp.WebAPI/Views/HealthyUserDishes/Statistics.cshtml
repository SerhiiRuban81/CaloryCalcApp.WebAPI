@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<CaloryCalcApp.Web.Models.DTOs.HealthyUserDishes.HealthyUserDishDTO>
<!-- Include Chart.js library -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<h1>Statistics</h1>

<!-- Form with dropdown -->
<form method="get" id="timeForm">
    <label for="daysSelect">Please select time period:</label>

    <select id="daysSelect" name="days" class="form-select" onchange="document.getElementById('timeForm').submit();">
        @foreach (var option in (List<SelectListItem>)ViewBag.TimeOptions)
        {
            <option value="@option.Value" selected="@(option.Value == ViewBag.SelectedDays)"> @option.Text </option>
        }
    </select>
</form>

<canvas id="CaloryCalcChart" width="800" height="400"></canvas>

<div class="card mb-4">
    <div class="card-header bg-primary text-white">
        <h4>Consumption Summary for the choosen period</h4>
    </div>
    <div class="card-body">
        <h2>Summary</h2>
        <div class="table-container rounded-3 overflow-hidden">
            <table class="table table-striped table-bordered table-hover ">
                <thead class="table-dark">
                    <tr>
                        <th>Metric</th>
                        <th>Total</th>
                        <th>Average per Day</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Calories (kcal)</td>
                        <td>@ViewBag.TotalCalories.ToString("0.###")</td>
                        <td>@ViewBag.AverageCaloriesPerDay.ToString("0.###")</td>
                    </tr>
                    <tr>
                        <td>Fats (g)</td>
                        <td>@ViewBag.TotalFats.ToString("0.###")</td>
                        <td>@ViewBag.AverageFatsPerDay.ToString("0.###")</td>
                    </tr>
                    <tr>
                        <td>Proteins (g)</td>
                        <td>@ViewBag.TotalProteins.ToString("0.###")</td>
                        <td>@ViewBag.AverageProteinsPerDay.ToString("0.###")</td>
                    </tr>
                    <tr>
                        <td>Carbohydrates (g)</td>
                        <td>@ViewBag.TotalCarbohydrates.ToString("0.###")</td>
                        <td>@ViewBag.AverageCarbohydratesPerDay.ToString("0.###")</td>
                    </tr>
                </tbody>
            </table>
        </div>        
    </div>
</div>



<script>
    // Parsing the JSON data passed from controller
    const dataPoints = JSON.parse('@Html.Raw(ViewBag.MealData)');

    // Preparing labels (dates)
    const labels = dataPoints.map(dp => dp.DateTime);

    // Preparing datasets
    const caloriesData = dataPoints.map(dp => dp.TotalCalories);
    const fatsData = dataPoints.map(dp => dp.TotalFats);
    const proteinsData = dataPoints.map(dp => dp.TotalProteins);
    const carbsData = dataPoints.map(dp => dp.TotalCarbohydrates);

    // Creating the chart
    const ctx = document.getElementById('CaloryCalcChart').getContext('2d');
    const myChart = new Chart(ctx, {
        type: 'line', // or 'bar', 'area', etc.
        data: {
            labels: labels,
            datasets: [
                {
                    label: 'Calories',
                    data: caloriesData,
                    borderColor: 'rgba(255, 99, 132, 1)',
                    backgroundColor: 'rgba(255, 99, 132, 0.2)',
                    fill: false,
                    tension: 0.4
                },
                {
                    label: 'Fats (g)',
                    data: fatsData,
                    borderColor: 'rgba(54, 162, 235, 1)',
                    backgroundColor: 'rgba(54, 162, 235, 0.2)',
                    fill: false,
                    tension: 0.4
                },
                {
                    label: 'Proteins (g)',
                    data: proteinsData,
                    borderColor: 'rgba(75, 192, 192, 1)',
                    backgroundColor: 'rgba(75, 192, 192, 0.2)',
                    fill: false,
                    tension: 0.4
                },
                {
                    label: 'Carbohydrates (g)',
                    data: carbsData,
                    borderColor: 'rgba(153, 102, 255, 1)',
                    backgroundColor: 'rgba(153, 102, 255, 0.2)',
                    fill: false,
                    tension: 0.4
                }
            ]
        },
        options: {
            responsive: true,
            plugins: {
                title: {
                    display: true,
                    text: 'Nutritional Data Over Time'
                },
                tooltip: {
                    mode: 'index',
                    intersect: false,
                },
                legend: {
                    position: 'top',
                }
            },
            interaction: {
                mode: 'nearest',
                axis: 'x',
                intersect: false
            },
            scales: {
                x: {
                    display: true,
                    title: {
                        display: true,
                        text: 'Date and Time'
                    }
                },
                y: {
                    display: true,
                    title: {
                        display: true,
                        text: 'Amount'
                    }
                }
            }
        }
    });
</script>




@* Old, but working well version with cards (only for small amount of data, just for check if we receiving data properly). *@
@* @foreach (var item in Model)
{
    <div class="card mb-3">
        <div class="card-body">
            <h5 class="card-title">@ViewBag.DishNames[item.DishId]</h5>
            <p class="card-text">Amount: @item.Amount</p>
			<p class="card-text">Calories, kkal: @ViewBag.DishCalories[item.MealTime]</p>

            <p class="card-text">Meal Time: @item.MealTime</p>
        </div>
    </div>
} *@
